services:
  frontend:
    build: .
    container_name: frontend
    restart: unless-stopped
    networks:
      - proxy-net
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
 
networks:
  proxy-net:
    external: true
